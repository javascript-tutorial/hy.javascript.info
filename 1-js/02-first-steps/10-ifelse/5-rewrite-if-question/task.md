կարևորությունը՝ 5

---

# «if»-ը փոխարինեք «?»-ով

Վերագրեք այս `if`-ը՝ օգտագործելով `'?'` պայմանական օպերատորը.

```js
let result;

if (a + b < 4) {
  result = 'Ցածր';
} else {
  result = 'Բարձր';
}
```
